library(TrajConserve); library(Seurat); small_epi <- readRDS("data/small_example.Rds"); trajectory_data <- seurat_to_trajectory_array(seurat_obj = small_epi, assay = "originalexp", pseudo_col = "pseudo", project_col = "Project"); gene_idx <- 10; model_test <- run_trajectory_model(trajectory_data$reshaped_data, gene_idx); plot_results_brms(model_test); print("Model test completed successfully!")
